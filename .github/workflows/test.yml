name: Flutter CI

on:
  push:
    branches: [ "main", "master" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
          channel: 'stable'
      
      - name: Install dependencies
        run: flutter pub get
        
      - name: Generate Mocks
        run: dart run build_runner build -d
        
      - name: Run specific successful tests
        # This guarantees it only runs the tests we know will pass!
        run: flutter test test/models/user_model_test.dart test/services/auth_service_test.dart test/firestore_rules_test.dart