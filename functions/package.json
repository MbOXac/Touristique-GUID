{
  "name": "touristique-guide-functions",
  "description": "Firebase Cloud Functions for Touristique GUID app",
  "version": "1.0.0",
  "engines": {
    "node": "20"
  },
  "main": "src/index.js",
  "scripts": {
    "serve": "functions-framework --target=geminiChat --debug --port=8080",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log --project=touristique-guide-2026"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "@google-cloud/functions-framework": "^3.0.0",
    "axios": "^1.6.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^0.2.0"
  }
}
